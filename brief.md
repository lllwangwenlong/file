# Daily Todo（日常待办事项）— 设计 Brief

> **版本**：v1.0 | **日期**：2026-09-17 | **目标用户**：内部用户  
> **基于**：`docs/superpowers/plans/2026-09-15-daily-todo.md`

---

## 1. 场景列表

| # | 场景名称 | 触发方式 | 用户意图 | 关键交互 | 预期结果 |
|---|---------|---------|---------|---------|---------|
| S1 | **首次访问空列表** | 导航至 `/todos` | 查看待办事项，发现列表为空 | 看到空状态引导文案 | 了解当前无待办，准备创建 |
| S2 | **快速创建事项** | 填写名称 → 回车 / 点击「添加」 | 用最少步骤记录一件事 | 仅填名称，描述留空，回车提交 | 列表即时追加新条目，表单清空 |
| S3 | **创建含描述的事项** | 填写名称 + 描述 → 点击「添加」 | 完整记录事项细节 | 输入名称和描述，点击提交 | 列表追加条目，名称和描述均展示 |
| S4 | **创建后继续添加** | 提交成功后表单自动清空 | 连续录入多条事项 | 连续 S2/S3 操作 | 列表持续累加，无阻塞感 |
| S5 | **访问已有数据的列表** | 导航至 `/todos`（非首次） | 回顾已记录的待办事项 | 页面加载时自动拉取列表 | 列表按创建时间倒序展示 |
| S6 | **名称为空校验** | 未填名称直接提交 | 误触或试探性提交 | 点击「添加」或回车 | 前端阻止提交，显示「事项名称不能为空」 |
| S7 | **后端不可用降级** | 后端未启动时访问页面 | 正常打开页面 | 页面静默渲染，GET 失败不阻断 UI | 表单可用，列表为空；创建时弹出友好错误提示 |
| S8 | **页面刷新保持** | 浏览器刷新 `/todos` | 确认数据持久性 | 刷新后自动 GET 拉取 | 之前创建的事项仍在列表中（后端内存保持） |

---

## 2. 时长分布

### 2.1 单场景交互时长预估

| 场景 | 用户操作耗时 | 系统响应耗时 | 总耗时 | 占比 |
|------|:----------:|:----------:|:-----:|:----:|
| S1 首次空列表 | 1s | ≤ 0.3s (GET) | 1.3s | 8% |
| S2 快速创建 | 3s | ≤ 0.2s (POST) + ≤ 0.2s (乐观追加) | 3.4s | 21% |
| S3 含描述创建 | 8s | ≤ 0.2s (POST) | 8.2s | 50% |
| S4 连续创建 | 3s × N | ≤ 0.2s × N | 3.2N s | — |
| S5 已有数据访问 | 2s | ≤ 0.3s (GET) | 2.3s | 14% |
| S6 空名校验 | 1s | 0s（前端拦截） | 1s | 6% |
| S7 后端降级 | 1s | ≤ 0.5s（超时失败） | 1.5s | — |
| S8 刷新保持 | 1s | ≤ 0.3s (GET) | 1.3s | — |

### 2.2 用户注意力分布

- **核心路径（80% 时间）**：S2 快速创建 + S3 含描述创建 + S4 连续创建  
  用户在表单区域停留时间最长，表单设计必须极简、无干扰。
- **辅助路径（15% 时间）**：S5 已有数据访问  
  列表可读性是次要优化目标。
- **边缘路径（5% 时间）**：S1 空状态、S6 校验、S7 降级  
  错误/空状态虽低频但直接影响信任感。

---

## 3. 视觉规范

### 3.1 设计原则
- **极简主义**：单一核心任务（创建），消除一切非必要视觉元素
- **克制配色**：以蓝色系为主操作色，灰色系为辅助，仅错误态使用红色
- **清晰层级**：表单区（操作）→ 列表区（结果），自上而下自然阅读流

### 3.2 色彩系统

| 用途 | 色值 | 说明 |
|------|------|------|
| 主操作色（按钮背景） | `#409eff` | Element UI 经典蓝，醒目且克制 |
| 主操作色 Hover | `#337ecc` | 深 1 档 |
| 主操作色 Disabled | `#a0cfff` | 浅蓝，明确不可用 |
| 文字主色 | `#333333` | 标题、事项名称 |
| 文字辅色 | `#555555` | 表单标签 |
| 文字次辅色 | `#777777` | 描述文本 |
| 文字弱色 | `#999999` | 计数、空状态 |
| 时间戳色 | `#bbbbbb` | 时间信息 |
| 错误色 | `#e74c3c` | 校验错误文字、必填星号 |
| 背景白 | `#ffffff` | 卡片背景 |
| 分割线 | `#f0f0f0` | 列表项分隔 |
| 输入框边框 | `#dddddd` | 默认 |
| 输入框聚焦 | `#409eff` | 与主操作色统一 |

### 3.3 字体层级

| 层级 | 字号 | 字重 | 用途 |
|------|:---:|:---:|------|
| H2 标题 | 18px | 400 | 表单区标题「新增待办事项」、列表区标题「待办事项列表」 |
| 事项名称 | 15px | 600 | 列表项名称 |
| 正文 / 表单标签 | 14px | 400 | 标签、输入框、按钮 |
| 描述文本 | 13px | 400 | 列表项描述 |
| 辅助信息 | 12px | 400 | 创建时间 |

### 3.4 间距与圆角

| 元素 | 值 |
|------|:---:|
| 页面最大宽度 | 720px |
| 页面水平 padding | 16px |
| 页面垂直 padding | 32px |
| 卡片内边距 | 24px |
| 卡片间距 | 24px |
| 表单组间距 | 14px |
| 标签与输入框间距 | 6px |
| 列表项内边距 | 14px 16px |
| 输入框/按钮内边距 | 10px 12px (输入), 10px 32px (按钮) |
| 卡片圆角 | 8px |
| 输入框/按钮圆角 | 4px |
| 卡片阴影 | `0 1px 4px rgba(0,0,0,0.08)` |

### 3.5 交互反馈

| 交互 | 反馈 |
|------|------|
| 输入框聚焦 | 边框色过渡至 `#409eff`（transition 0.2s） |
| 按钮 Hover（可用态） | 背景色过渡至 `#337ecc` |
| 按钮 Disabled | 背景色变浅 `#a0cfff`，cursor: not-allowed |
| 空名校验 | 按钮保持 disabled 无法点击；若通过回车触发则显示红色错误提示 |
| 创建中 | 表单下方显示灰色「加载中...」 |
| 创建失败 | `alert()` 弹窗（MVP 方案，后续迭代可替换为 toast） |
| 列表为空 | 居中灰色文字「暂无待办事项，请在上方添加。」 |

### 3.6 响应式策略
- 最大宽度 720px，水平居中
- 移动端（< 720px）：表单和列表撑满屏幕宽度，padding 16px
- 不做断点拆分（MVP 阶段，内部用户桌面端为主）

---

## 4. 信息架构

### 4.1 页面层级

```
/todos (待办事项页)
├── TodoView（页面容器）
│   ├── TodoForm（创建表单区）
│   │   ├── 标题：「新增待办事项」
│   │   ├── 输入框：事项名称（必填，placeholder: 请输入事项名称）
│   │   ├── 文本域：事项描述（选填，placeholder: 请输入事项描述（选填））
│   │   ├── 按钮：「添加」（名称非空时可用）
│   │   └── 错误提示（条件渲染，红色）
│   └── TodoList（列表展示区）
│       ├── 标题：「待办事项列表（共 N 项）」
│       ├── 空状态（条件渲染）
│       └── 列表项 × N
│           ├── 事项名称
│           ├── 事项描述（条件渲染）
│           └── 创建时间（格式化：YYYY-MM-DD HH:mm）
```

### 4.2 路由结构

```
/todos                     → TodoView（懒加载）
  - meta.title: "待办事项"
  - mode: history
```

### 4.3 数据流

```
用户输入 → TodoForm (emit submit)
  → TodoView.handleCreate()
    → store.dispatch('todos/createTodo', { name, description })
      → api.createTodo({ name, description })
        → axios POST /todos → Express → 内存数组
      ← 返回 Todo 对象
    → commit('todos/ADD_TODO', todo)
  → TodoList (computed: todos) 自动更新

页面加载：
  TodoView.mounted()
    → store.dispatch('todos/fetchTodos')
      → api.fetchTodos()
        → axios GET /todos → Express → 内存数组
      ← 返回 Todo[]
    → commit('todos/SET_TODOS', todos)
  → TodoList 自动渲染
```

### 4.4 状态设计

| 状态 | 前端表现 |
|------|---------|
| 加载中（初始化） | 列表区底部灰色「加载中...」 |
| 空列表 | 列表区居中「暂无待办事项，请在上方添加。」 |
| 有数据 | 列表渲染事项卡片 |
| 创建中 | TodoView.loading = true，列表区底部「加载中...」 |
| 创建成功 | 表单清空，列表即时追加 |
| 创建失败 | alert 弹窗，表单内容保留 |
| 后端不可用 | GET 静默失败（空列表），POST 弹出错误提示 |

---

## 5. 需展示的真实数据素材清单

### 5.1 Todo 项数据字段

| 字段 | 类型 | 来源 | 示例值 | 展示位置 |
|------|------|------|--------|---------|
| `id` | `number` | 后端自增 | `1` | 不可见（用作 `:key`） |
| `name` | `string` | 用户输入（必填） | `"完成 Q3 季度报告"` | 列表项名称 |
| `description` | `string` | 用户输入（选填） | `"包含收入、成本、利润三部分分析"` | 列表项描述（仅非空时展示） |
| `createdAt` | `string` | 后端生成 | `"2026-09-17T10:30:00.000Z"` | 列表项时间（格式化后） |

### 5.2 API 响应格式

**GET /todos 成功：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "name": "完成 Q3 季度报告",
      "description": "包含收入、成本、利润三部分分析",
      "createdAt": "2026-09-17T02:30:00.000Z"
    }
  ]
}
```

**POST /todos 成功：**
```json
{
  "code": 201,
  "data": {
    "id": 2,
    "name": "预约牙医",
    "description": "",
    "createdAt": "2026-09-17T03:15:00.000Z"
  }
}
```

**POST /todos 校验失败：**
```json
{
  "code": 400,
  "message": "待办事项名称不能为空"
}
```

### 5.3 前端时间格式化

```
输入：ISO 8601 字符串 "2026-09-17T02:30:00.000Z"
输出：本地时间格式化 "2026-09-17 10:30"
```

### 5.4 演示用种子数据（可选，不自动注入）

若后续需要演示环境，建议预置 3–5 条真实风格的示例数据：

| name | description |
|------|-------------|
| 完成 Q3 季度报告 | 包含收入、成本、利润三部分分析，周五前提交 |
| 预约牙医 | 市口腔医院，电话 021-xxxxxxx |
| 整理周会纪要 | 9月15日会议记录，发给全组 |
| 更新项目 README | 补充部署文档和 API 说明 |

---

## 附录：已确认的实现约束

- 最小闭环：仅创建，不含编辑 / 删除 / 标记完成
- 后端存储：内存数组（服务重启后数据丢失）
- 前端路由模式：history
- 无鉴权（内部工具）
- 无国际化需求
- 无无障碍访问（a11y）专项要求（MVP 阶段）
- 加载进度条：复用已有 Nprogress（路由 beforeEach/afterEach 已配置）